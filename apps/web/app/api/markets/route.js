export async function GET(){try{const symbols=encodeURIComponent(JSON.stringify(['BTCUSDT','ETHUSDT','SOLUSDT']));const r=await fetch('https://data-api.binance.vision/api/v3/ticker/24hr?symbols='+symbols,{next:{revalidate:60},signal:AbortSignal.timeout(8000)});if(!r.ok)throw Error();const rows=await r.json();return Response.json({updatedAt:new Date().toISOString(),markets:rows.map(r=>({symbol:r.symbol,name:({BTCUSDT:'Bitcoin',ETHUSDT:'Ethereum',SOLUSDT:'Solana'})[r.symbol],price:+r.lastPrice,change:+r.priceChangePercent,high:+r.highPrice,low:+r.lowPrice,volume:+r.quoteVolume})),source:'Binance · 24h snapshot'})}catch{return Response.json({markets:[],error:'Market prices are temporarily unavailable.'},{status:503})}}
